rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Pastikan pengguna hanya bisa membaca dan menulis data jika mereka sudah login
      allow read, write: if request.auth != null;
    }
  }
}